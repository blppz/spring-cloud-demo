server:
  port: 5900

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/blppz/config-center.git/
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://eureka-7901:7901/eureka/